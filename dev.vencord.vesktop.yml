app-id: dev.vencord.vesktop
base: org.electronjs.Electron2.BaseApp
base-version: "22.08"
runtime: org.freedesktop.Platform
runtime-version: "22.08"
sdk: org.freedesktop.Sdk
separate-locales: false
command: dev.vencord.vesktop
rename-desktop-file: vesktop.desktop
finish-args:
  - --share=ipc
  - --socket=x11
  - --socket=wayland
  - --socket=pulseaudio
  - --share=network
  - --device=all
  - --talk-name=org.freedesktop.Notifications
  - --filesystem=xdg-run/app/com.discordapp.Discord:create
  - --own-name=org.mpris.MediaPlayer2.vesktop
  - --talk-name=org.gnome.Mutter.IdleMonitor
  - --talk-name=org.kde.StatusNotifierWatcher
  - --talk-name=com.canonical.AppMenu.Registrar
  - --talk-name=com.canonical.indicator.application
  - --talk-name=org.ayatana.indicator.application
  - --talk-name=org.freedesktop.portal.Background
modules:
  - name: vesktop
    buildsystem: simple
    build-commands:
      - chmod +x Vesktop-0.3.2.AppImage
      - ./Vesktop-0.3.2.AppImage --appimage-extract

      - install -dm755 "${FLATPAK_DEST}/bin" "${FLATPAK_DEST}/lib/vesktop"
      - cp -r squashfs-root/* "${FLATPAK_DEST}/lib/vesktop"
      - cp -r squashfs-root/usr/lib/* "${FLATPAK_DEST}/lib"

      - install -Dm644 "dev.vencord.vesktop.png" "${FLATPAK_DEST}/share/icons/hicolor/512x512/apps/dev.vencord.vesktop.png"
      - install -Dm755 "run.sh" "${FLATPAK_DEST}/bin/dev.vencord.vesktop"
      - desktop-file-install --dir="${FLATPAK_DEST}/share/applications" --remove-key=X-AppImage-Version --set-key=Exec --set-value=dev.vencord.vesktop --set-key=Icon --set-value=dev.vencord.vesktop vesktop.desktop

      - install -Dm644 -t ${FLATPAK_DEST}/share/metainfo dev.vencord.vesktop.metainfo.xml
    sources:
      - type: file
        url: https://github.com/Vencord/Vesktop/releases/download/v0.3.2/Vesktop-0.3.2.AppImage
        sha256: 10a2a710dd16abc36e8a78b51d4410e8eaf83c2ec8e6c11a60abb2a99620095e
      - type: file
        path: dev.vencord.vesktop.metainfo.xml
      - type: file
        path: dev.vencord.vesktop.png
      - type: file
        path: vesktop.desktop
      - type: file
        path: run.sh
